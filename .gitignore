# Fish Shell
home/.config/fish/fish_variables

# Claude (agent-local working dir: worktrees, notes, local settings) --
# except commands/, which is shared project tooling and stays tracked
.claude/*
!.claude/commands/

# User scratch notes / temporary files
.tmp/

# Copyright (c) 2026 Xavier Beheydt <xavier.beheydt@gmail.com>
#
# Doc: https://wiki.hypr.land/hypr-ecosystem/user/hypridle/
#
# TODO: temporary config, needs a proper rework:
#   - wire up the lock (hyprlock) via lock_cmd / before_sleep_cmd / listener below
#   - replace with a quickshell control interface (idle/lock/dpms)

general {
    # lock_cmd = pidof hyprlock || hyprlock
    # before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
}

# listener {
#     timeout = 300
#     on-timeout = loginctl lock-session
# }

listener {
    timeout = 300
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable" })'
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
}

listener {
    timeout = 600
    on-timeout = systemctl sleep
}

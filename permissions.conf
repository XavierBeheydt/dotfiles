# path                                     owner     group     mode
#
# One entry per file/dir tracked in this repo. Generated/updated
# automatically by `dotfiles add <path>`.
#
#   path   is repo-relative and starts with etc/ or home/
#          (e.g. etc/nginx/nginx.conf, home/.ssh/config)
#   owner  and group are restored on both /etc and home targets by
#          bootstrap (as root for /etc, as your user for home)
#   mode   is restored the same way, as octal (e.g. 600, 644, 750)
#
# Files not listed here keep whatever mode git/the filesystem gives
# them (default 644, or 755 if the executable bit is set).
#
# home/.config and etc/ dotfiles are being re-added one at a time (see
# the Project board); each one gets its entry back here as it's
# re-tracked.
home/.config/fish                         xavier    xavier    700
home/.config/fish/conf.d                  xavier    xavier    700
home/.config/fish/conf.d/aliases.fish     xavier    xavier    644
home/.config/fish/conf.d/env.fish         xavier    xavier    644
home/.config/fish/functions               xavier    xavier    700
home/.config/fish/functions/copy.fish     xavier    xavier    644
home/.config/fish/functions/extract.fish  xavier    xavier    644
home/.config/fish/functions/fish_greeting.fish  xavier    xavier    644
home/.config/fish/functions/history.fish  xavier    xavier    644
home/.config/fish/functions/mkcd.fish     xavier    xavier    644
home/.config/fish/config.fish             xavier    xavier    644
home/.config/user-dirs.dirs                xavier    xavier    600
home/.config/user-dirs.locale              xavier    xavier    644
etc/pam.d/greetd                          root      root      644
etc/ssh/sshd_config.d/99-archlinux.conf   root      root      644
etc/systemd/system-sleep/nvidia-hdmi-audio-fix  root  root    755
home/.config/uwsm                         xavier    xavier    755
home/.config/uwsm/env                     xavier    xavier    644
home/.config/uwsm/env-hyprland            xavier    xavier    644
